import { createContext } from "react";

const LoginContext = createContext({});
const adminContext = createContext({});
const userIDContext = createContext({});


export { LoginContext, adminContext, userIDContext };
